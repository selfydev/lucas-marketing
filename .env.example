# Database Configuration
# Same database as main server for shared auth sessions
DATABASE_URL=

# Better Auth Configuration
# MUST match apps/server BETTER_AUTH_SECRET (same auth, shared sessions)
BETTER_AUTH_SECRET=

# App URLs
VITE_APP_URL=http://localhost:3001
